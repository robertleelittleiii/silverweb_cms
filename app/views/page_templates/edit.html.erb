
  <div class="hidden-item">
    <div id="page-template-id"><%=@page_template.id%></div>
  </div>

  <div class="cms-block" Align="Left">
    <div align="center">
      <!-- START:flash -->
      <% if notice -%>
        <div id="notice"><%= notice  %></div>
      <% end -%>
      <!-- END:flash -->

      <% if @page_template.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@page_template.errors.count, "error") %> prohibited this page_template from being saved:</h2>

          <ul>
            <% @page_template.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

    </div>

  <h1 class="cms-header">Edit Page Template</h1>

        <%= render 'form' %>

      </p>
    </div>

  <p> <%= link_to '<< Back', request.env['HTTP_REFERER'], :class=>"button-link" %></p>





