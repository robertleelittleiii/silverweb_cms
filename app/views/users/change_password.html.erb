
<div class="hidden-item" id="user-id"><%=@user.id%></div>
<div class="hidden-item">
  <div id="as_window"><%=params[:as_window]%></div>
  <div id="dialog-height">345</div>
  <div id="dialog-width">365</div>
  <div id="dialog-name">Change Password</div>
</div>

<div class="m-block" Align="Left">
  <div align="center">
    <!-- START:flash -->
    <% if notice -%> 
      <div id="notice"><%= notice  %></div>
    <% end -%>
    <!-- END:flash -->

    <% if @user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
          <% @user.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

  </div>
  <%  if params[:as_window]==false then%>

  <div class="m-header">Change Password</div>
  <% end %>
  <div class="m-section">
    <div class="m-heading">Please enter new password for <%=@user.name%></div>
    <div class="m-content">
      <%= form_for @user, :url => { :action => "update_password" , :id=>@user.id}, :html => {:id=>"update-password-form",:remote=>true} do |f| %>
      <div id="password-div"> Password: <%= f.password_field :password %> </br></br></div>
      <div id="reenter-div">Re-enter: <%= f.password_field :password_confirmation %> </br></div>
        <%= f.submit "Update", :id=>"update-password-button", :class=>"button-link"%>
      <% end %>      
    </div>
    </p>
  </div>

  <%  if params[:as_window]==false then%>
    <%= link_to 'Show', @user %> |
  <%= link_to 'Back', users_path %>
  <% end %>

</div>