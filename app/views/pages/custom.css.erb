<%#= render :file=>"/public/stylesheets/global-reset.css" %>
<%#= render :file=>Rails.application.assets['fonts.css'].pathname.to_s%>

<%=render :text=>File.read(Rails.root.to_s + ActionController::Base.helpers.compute_asset_path('fonts.css'))%>

// tiny mce body override
html body#tinymce{
    background-color: white;
}
<%=render :text=>File.read(Rails.root.to_s + ActionController::Base.helpers.compute_asset_path('website.css')) %>
<%=(@page.template_name.blank? ? "" : render(:text=>File.read(Rails.root.to_s + ActionController::Base.helpers.compute_asset_path("site/show_page-#{@page.template_name}.css")))) rescue ""%>
<%=render(:text=>File.read(Rails.root.to_s + ActionController::Base.helpers.compute_asset_path("editor-custom.css"))) %>
<%=render(:text=>File.read(Rails.root.to_s + ActionController::Base.helpers.compute_asset_path("#{@page.page_style}")))  rescue ""%>


// tiny mce body override

html body#tinymce{
    background-color: white;
}
